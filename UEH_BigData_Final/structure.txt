project-root/
│
├── README.md
├── requirements.txt
├── setup.cfg
├── pyproject.toml
├── Makefile
│
├── data/
│   ├── external/   # Dữ liệu bên ngoài (nếu có)
│   ├── interim/    # Dữ liệu trung gian (bước chuyển đổi, chuẩn hóa tạm thời)
│   ├── processed/  # Dữ liệu đã xử lý, sẵn sàng cho modeling
│   └── raw/        # Dữ liệu gốc, đã crawl từ FastF1 (theo từng mùa giải, từng GP)
│
├── docs/           # Tài liệu hướng dẫn, mô tả dự án
│
├── models/         # Model đã train, checkpoint, file dự đoán
│
├── notebooks/      # Notebook phân tích, EDA, pipeline, thử nghiệm
│
├── references/     # Data dictionary, tài liệu giải thích, bản đồ cột dữ liệu
│
├── reports/        # Báo cáo tự động, kết quả, hình ảnh
│   └── figures/
│
└── src/            # Source code chính cho toàn bộ dự án
    ├── __init__.py
    ├── config.py           # Biến cấu hình chung (đường dẫn, năm, tham số Spark...)
    ├── data/
    │   ├── __init__.py
    │   ├── f1_crawler.py   # Class F1DataCollector và các hàm crawl dữ liệu
    │   ├── crawl_season.py # Script crawl toàn bộ mùa giải, lưu vào data/raw/
    │   └── utils.py        # Hàm phụ trợ (chuẩn hóa tên file, logging, ... nếu cần)
    ├── features/
    │   ├── __init__.py
    │   └── build_features.py # Tạo feature từ dữ liệu raw/interim
    ├── modeling/
    │   ├── __init__.py
    │   ├── train.py        # Huấn luyện mô hình Spark MLlib
    │   └── predict.py      # Dự đoán với model đã train
    └── visualization/
        ├── __init__.py
        └── plots.py        # Hàm vẽ biểu đồ, trực quan hóa dữ liệu
